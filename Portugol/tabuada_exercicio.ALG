Algoritmo "tabuada"

Var

   n, i, mult : inteiro

Inicio

   escreva("Deseja a tabuada para qual valor? ")
   leia(n)
   
   para i de 1 ate 10 faca
   
      mult <- n * i
      escreval(n, " x", i, " =", mult)
      
   fimpara

Fimalgoritmo